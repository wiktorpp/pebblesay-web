To install this program open terminal, navigate to this folder, and type:

    sudo cp nishisay /usr/bin
    sudo chmod +x /usr/bin/nishisay
    sudo chmod 777 /usr/bin/nishisay

The original drawing was drawn by Nishi (Julia). Thx for letting me use it ^^